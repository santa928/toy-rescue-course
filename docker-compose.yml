services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "5180:5173"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    command: npm run dev

volumes:
  node_modules:
